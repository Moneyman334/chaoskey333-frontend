import { useContext } from 'react';
import { AnalyticsProvider } from '../providers/AnalyticsProvider';

// Re-export the analytics hook for convenience
export { useAnalytics } from '../providers/AnalyticsProvider';